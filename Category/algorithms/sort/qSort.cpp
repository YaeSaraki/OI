#include <algorithm>
#include <iostream>
using namespace std;

string number;
void quickSort(string& str, int n);


int main() {
  cin >> number;
  quickSort(number, number.size());
  cout << number;
  return 0;
}

void quickSort(string& str, int n) {
    
}