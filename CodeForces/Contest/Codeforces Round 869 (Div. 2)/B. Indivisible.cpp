#include <algorithm>
#include <cstring>
#include <iostream>
#define endl '\n'
using namespace std;

const int kN = 101;
int a[kN], sum[kN];

inline void solve() {
  
}

bool rt = false;
int32_t main() {
  ios::sync_with_stdio(0); cin.tie(0); cout.tie(0);
  #ifndef ONLINE_JUDGE
  freopen("test.in", "r", stdin);
  #endif
  if (rt) {int T; cin >> T; while (T--) solve(); }
  else solve();
  return 0;
}